/** Default restaurant used for local/dev QR simulation. */
export const DEFAULT_RESTAURANT_SLUG = 'demo-bistro';

/** Shown when the API returns a null imageUrl. */
export const DEFAULT_MENU_IMAGE_URL =
  'https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=800&q=80';

/** Default prep time until the backend provides one. */
export const DEFAULT_PREP_MINUTES = 15;
